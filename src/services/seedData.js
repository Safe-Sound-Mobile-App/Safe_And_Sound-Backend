/**
 * Seed Data Service
 * Placeholder - seeding disabled, users come from Firebase
 */

function seedSampleData() {
    console.log('ℹ️  Seeding skipped - users will be registered via Firebase.');
}

module.exports = { seedSampleData };
